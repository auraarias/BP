let name = document.getElementById("formName");
let lastName = document.getElementById("formLastName");
let csv = document.getElementById("csv");
let generar = document.getElementById("generar");

generar.addEventListener('click', () => {
    //Fetch
    // Parseas a JSON
    // Asigna Variabes
    //Llenas los inputs con las variables como esta abajo
    name.value = csv.value;
    csv.value = "";
    lastName.value = "arias";
})

hour, sys, dia,
8:00, 120, 60
8:00, 120, 60
8:00, 120, 60
8:00, 120, 60
8:00, 120, 60

